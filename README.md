# Swapping variables using XOR Swap algorithm 

The algortihm in short is  :-

    X := X XOR Y
    Y := Y XOR X
    X := X XOR Y

for more information [Wikipedia](https://en.wikipedia.org/wiki/XOR_swap_algorithm)